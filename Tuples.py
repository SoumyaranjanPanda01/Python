t = tuple(range(10))
print(t)

# tuples are immutable
# any type of elements can be stored inside a tuple
# elements are ordered
# duplicate elements are allowed
# operations like (add, update, delete) can't be performed
# operations like (indexing, slicing, iteration) can be performed
# sorting not possible
